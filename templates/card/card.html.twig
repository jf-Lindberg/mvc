{% extends "base.html.twig" %}

{% block title %}{{ title }}{% endblock %}

{% block content %}
    <div class="container">
        <h3>Card</h3>
        <a href="{{ path('card-deck') }}">Deck</a><br/>
        <a href="{{ path('card-shuffle') }}">Shuffle</a><br/>
        <a href="{{ path('card-draw') }}">Draw</a><br/>
    </div>
{% endblock %}
